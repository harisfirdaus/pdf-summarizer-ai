{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/$1"
    }
  ],
  "outputDirectory": "dist",
  "buildCommand": "cd pdf-summarizer-gemini && npm install && npm run build",
  "installCommand": "cd pdf-summarizer-gemini && npm install"
}